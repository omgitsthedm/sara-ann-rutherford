# AI Tool Entry

Read `.ai/RULES.md` and `.ai/STATE.md` before working, then `CLAUDE.md`.
The shared project rules live in `.ai/RULES.md`; current state in `.ai/STATE.md`.

PLACEHOLDER repo (static artist site, branch `master`) — real source still in iCloud `02_CLIENTS`, Netlify deploy linkage unverified. Do not push/deploy, modify secrets, or mutate production without the required gate (clear, scoped confirmation from David). `git push` = production deploy. Before pushing real content, add a `/.ai/*` + `/CLAUDE.md` + `/AGENTS.md` → 404 exclusion to the new `netlify.toml`.
